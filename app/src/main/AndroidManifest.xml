<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uts_mobile.profil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_fix"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_fix"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.uts_mobile.profil.MantanDetail"></activity>
        <activity android:name="com.uts_mobile.profil.AboutActivity" />
        <activity android:name="com.uts_mobile.profil.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>